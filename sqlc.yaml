version: "2"
sql:
  - engine: "postgresql"
    queries: "data/queries"
    schema: "data/schema.sql"
    gen:
      go:
        package: "db"
        out: "src/backend/db"
        sql_package: "pgx/v5"
